# ClawXXX Skill: Kubernetes Pilot
skill: kubernetes-pilot
version: "1.0"
mcp:
  server: mcp://clawxxx.skills/k8s/v1
  transport: stdio
  auth:
    type: kubeconfig
    path: ~/.kube/config
actions:
  - apply_manifest
  - get_pods
  - scale_deployment
  - read_logs
