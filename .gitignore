node_modules/
dist/
*.db
.env
.env.local
*.log

# SQLite database
openclaw.db
openclaw.db-shm
openclaw.db-wal
*.zip

# Canary token system — local manifest (do not commit; commit only for CI use)
.canary-manifest.json
